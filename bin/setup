#!/bin/bash

# todo: check for existence of parse cli
install_parse

# todo: load in `.env` file,
# check for existence of PARSE_APP_ID and PARSE_MASTER_KEY
setup_parse

# todo: check for existence of `npm`
npm install

# todo: check for existence of `grunt`
grunt

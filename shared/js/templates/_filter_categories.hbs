<ul class="filter-categories btn-group">
  {{#each categories}}
    <li class="unselectable">
      <div class="category btn" data-value="{{key}}">
        <i class="category-icon icon-{{icon}}"></i>{{title}}
        <div class="chevron-container">
          <i class="icon-right-open-2 chevron"></i>
        </div>
      </div>
    </li>
  {{/each}}
</ul>

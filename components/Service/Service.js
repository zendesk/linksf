import React, { PropTypes } from 'react'
import './Service.scss'

const Service = (props) => {
  const { service } = props
    return (
      <div>
        <div className="inset">
          service
        </div>
        description
      </div>
    )
  }

export default Service
